<UserControl x:Class="Net10.Wpf.MinimalAPI.IssueBoard.Views.IssueCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel Margin="15">

        <!-- タイトル -->
        <TextBlock DockPanel.Dock="Top"
                   Text="課題新規登録"
                   FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- エラー表示 + ボタン (常に画面下部) -->
        <StackPanel DockPanel.Dock="Bottom" Margin="0,8,0,0">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red" FontWeight="Bold"
                       TextWrapping="Wrap"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                       Margin="0,0,0,6"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="登録"
                        Command="{Binding RegisterCommand}"
                        Width="110" Height="32" Margin="6,0"/>
                <Button Content="戻る"
                        Command="{Binding BackCommand}"
                        Width="110" Height="32" Margin="6,0"/>
            </StackPanel>
        </StackPanel>

        <!-- 入力フォーム (スクロール可) -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 記入者氏名 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="記入者氏名 *:"
                           Style="{StaticResource FormLabelStyle}"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding AuthorName, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FormTextBoxStyle}"/>

                <!-- カテゴリ名 -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="カテゴリ名:"
                           Style="{StaticResource FormLabelStyle}"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FormTextBoxStyle}"/>

                <!-- 課題タイトル -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="課題タイトル *:"
                           Style="{StaticResource FormLabelStyle}"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FormTextBoxStyle}"/>

                <!-- 課題の文面 -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="課題の文面 *:"
                           Style="{StaticResource FormLabelStyle}"/>
                <TextBox Grid.Row="3" Grid.Column="1"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" Height="200" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Style="{StaticResource FormTextBoxStyle}"/>
            </Grid>
        </ScrollViewer>

    </DockPanel>
</UserControl>
